# 概念股映射系统深度优化方案

## 一、问题诊断

### 当前实现的问题
1. **映射数据硬编码** - 所有映射关系写死在LANGUAGE_STOCK_MAP、DOMAIN_STOCK_MAP、TECH_STACK_MAP中
2. **关键词匹配浅薄** - 只检查description/name中是否包含关键词，无语义理解
3. **无产业链视角** - 例如"AI推理框架"项目，推荐所有AI芯片股，但实际只影响推理端
4. **零验证机制** - 无法判断推荐的股票是否真的受项目影响
5. **无学习能力** - 映射关系固定，无法根据历史数据优化

### 用户痛点
- 推荐的概念股"好像都对，但不够准"
- 不知道为什么推荐这只股票（理由太笼统）
- 无法判断推荐的可靠性

---

## 二、优化架构设计

### 2.1 五层映射模型

```
用户需求：GitHub项目 → 概念股投资机会

第1层：项目特征提取（Project Feature Extraction）
├─ 技术特征：编程语言、技术栈、框架、工具
├─ 业务特征：应用场景、目标用户、商业模式
├─ 产业链特征：上游/中游/下游、技术环节
└─ 情绪特征：热度、增长趋势、社区活跃度

第2层：产业链知识图谱（Industry Chain Knowledge Graph）
├─ 技术领域 → 产业链环节映射
├─ 产业链环节 → 核心公司映射
├─ 公司 → 股票代码映射
└─ 竞争格局 → 龙头/跟随者/挑战者

第3层：动态映射引擎（Dynamic Mapping Engine）
├─ 多维度评分：技术相关性(30%) + 业务匹配度(25%) + 产业链位置(20%) + 市场情绪(15%) + 技术壁垒(10%)
├─ 动态权重调整：根据历史验证数据自动调整权重
└─ 置信度计算：高/中/低置信度标签

第4层：验证与反馈系统（Validation & Feedback System）
├─ 历史验证：追踪推荐后股价表现
├─ 用户反馈：采纳/忽略/反对投票机制
├─ 自动评估：计算推荐准确率、召回率
└─ 模型迭代：根据反馈优化映射规则

第5层：输出与展示（Output & Presentation）
├─ 推荐股票列表（Top 5）
├─ 多维度理由展示
├─ 置信度标签
├─ 风险提示
└─ 相关数据来源
```

---

## 三、核心优化点

### 3.1 产业链知识图谱构建

**数据结构示例：**
```python
INDUSTRY_CHAIN = {
    "AI": {
        "训练端": {
            "keywords": ["training", "model training", "deep learning training"],
            "stocks": [
                {"name": "寒武纪", "code": "688256", "role": "训练芯片", "market_share": "20%", "strength": "strong"},
                {"name": "海光信息", "code": "688041", "role": "训练芯片", "market_share": "15%", "strength": "medium"}
            ]
        },
        "推理端": {
            "keywords": ["inference", "model serving", "onnx", "tensorrt"],
            "stocks": [
                {"name": "寒武纪", "code": "688256", "role": "推理芯片", "market_share": "25%", "strength": "strong"},
                {"name": "海光信息", "code": "688041", "role": "推理芯片", "market_share": "18%", "strength": "medium"}
            ]
        },
        "数据端": {
            "keywords": ["data pipeline", "etl", "data preprocessing"],
            "stocks": [
                {"name": "东方财富", "code": "300059", "role": "数据处理", "strength": "medium"},
                {"name": "恒生电子", "code": "600570", "role": "数据服务", "strength": "medium"}
            ]
        },
        "应用端": {
            "keywords": ["ai application", "chatbot", "ai assistant"],
            "stocks": [
                {"name": "科大讯飞", "code": "002230", "role": "AI应用", "strength": "strong"},
                {"name": "云从科技", "code": "688327", "role": "AI应用", "strength": "medium"}
            ]
        }
    }
}
```

**优势：**
- 区分产业链不同环节，推荐更精准
- 考虑市场份额，优先推荐龙头
- 动态更新，支持人工修正

---

### 3.2 动态映射数据库

**数据库表设计：**

```sql
-- 映射规则表
CREATE TABLE mapping_rules (
    id INTEGER PRIMARY KEY,
    source_type VARCHAR(50),  -- 'language', 'domain', 'tech_stack', 'industry_chain'
    source_value VARCHAR(100),  -- 'python', 'AI', 'pytorch', 'AI.训练端'
    target_stock VARCHAR(50),  -- '寒武纪'
    target_code VARCHAR(10),  -- '688256'
    weight DECIMAL(3,2),  -- 权重 0.00-1.00
    confidence DECIMAL(3,2),  -- 置信度 0.00-1.00
    reason TEXT,  -- 映射理由
    verified_count INTEGER DEFAULT 0,  -- 验证次数
    success_count INTEGER DEFAULT 0,  -- 成功次数
    last_updated TIMESTAMP,
    created_at TIMESTAMP
);

-- 验证记录表
CREATE TABLE validation_records (
    id INTEGER PRIMARY KEY,
    repo_name VARCHAR(200),
    stock_name VARCHAR(50),
    stock_code VARCHAR(10),
    recommend_date TIMESTAMP,
    recommend_score DECIMAL(3,2),
    price_on_recommend DECIMAL(10,2),
    price_after_7d DECIMAL(10,2),
    price_after_30d DECIMAL(10,2),
    user_feedback VARCHAR(20),  -- 'adopted', 'ignored', 'rejected'
    created_at TIMESTAMP
);

-- 用户反馈表
CREATE TABLE user_feedback (
    id INTEGER PRIMARY KEY,
    repo_name VARCHAR(200),
    stock_name VARCHAR(50),
    feedback_type VARCHAR(20),  -- 'upvote', 'downvote', 'comment'
    comment TEXT,
    user_id VARCHAR(50),
    created_at TIMESTAMP
);
```

**优势：**
- 映射规则可动态更新，无需改代码
- 支持验证和学习
- 用户反馈驱动优化

---

### 3.3 多维度评分增强

**评分维度：**

| 维度 | 权重 | 评分标准 | 数据来源 |
|------|------|----------|----------|
| **技术相关性** | 30% | 技术栈匹配度、技术领域重叠度 | 项目README、依赖文件 |
| **业务匹配度** | 25% | 应用场景匹配、目标用户重叠 | 项目描述、文档 |
| **产业链位置** | 20% | 上/中/下游定位、核心环节判断 | 知识图谱 |
| **市场情绪** | 15% | 项目热度、增长趋势、社区活跃 | GitHub Stars/Forks、趋势数据 |
| **技术壁垒** | 10% | 技术难度、创新性、护城河 | 项目复杂度、引用数 |

**评分公式：**
```python
final_score = (
    tech_relevance * 0.30 +
    business_match * 0.25 +
    industry_chain_position * 0.20 +
    market_sentiment * 0.15 +
    tech_barrier * 0.10
)
```

**动态权重调整：**
- 根据历史验证数据，自动调整各维度权重
- 例如：如果"技术相关性"维度的验证成功率低，降低其权重
- 使用多臂老虎机算法（MAB）优化权重

---

### 3.4 验证与反馈系统

**验证流程：**
```
推荐股票 → 记录推荐时的股价 → 7天后/30天后检查股价 → 计算收益 → 更新验证数据
```

**用户反馈机制：**
1. **投票系统**：用户可以对推荐结果投票（👍有用/👎没用）
2. **评论系统**：用户可以补充说明为什么采纳/拒绝
3. **采纳追踪**：用户点击股票代码查看详情 → 记录为"采纳"

**自动评估指标：**
- **准确率**：推荐后股价上涨的比例
- **召回率**：真正上涨的股票中被推荐的比例
- **覆盖率**：推荐股票覆盖的产业链环节
- **时效性**：推荐到股价反应的时间

---

## 四、实施路径

### Phase 1：基础设施（1-2周）
- [ ] 设计数据库表结构
- [ ] 搭建SQLite数据库（轻量级，后续可迁移到PostgreSQL）
- [ ] 实现映射规则管理API（增删改查）
- [ ] 构建产业链知识图谱初始数据（AI、云计算、金融、游戏等领域）

### Phase 2：核心功能（2-3周）
- [ ] 实现五层映射模型
- [ ] 集成多维度评分引擎
- [ ] 开发验证系统（自动追踪股价）
- [ ] 实现用户反馈接口

### Phase 3：优化迭代（持续）
- [ ] 收集用户反馈数据
- [ ] 训练权重优化模型
- [ ] 定期更新产业链知识图谱
- [ ] 发布准确率报告

---

## 五、技术选型

### 数据库
- **初期**：SQLite（轻量级，无需额外服务）
- **长期**：PostgreSQL（支持更复杂的查询和扩展）

### NLP增强（可选）
- **文本理解**：使用OpenAI API或本地模型（如sentence-transformers）进行语义匹配
- **实体识别**：识别项目描述中的技术实体、公司名、产品名

### 数据源
- **股价数据**：新浪财经API（实时）、Tushare（历史数据）
- **产业链数据**：艾瑞咨询、艾媒咨询报告，券商研报
- **市场情绪**：GitHub API、社交媒体数据

---

## 六、预期效果

### 量化指标
- **推荐准确率**：从当前约50%提升至70%+
- **用户采纳率**：从当前未知提升至40%+
- **推荐理由清晰度**：从"笼统描述"升级为"多维度理由+置信度"
- **用户满意度**：通过投票系统量化

### 用户体验提升
- 推荐更精准，减少"好像都对但不够准"的感觉
- 理由更充分，用户能理解为什么推荐这只股票
- 有置信度标签，用户能判断推荐的可靠性
- 有验证数据支持，建立信任感

---

## 七、风险与应对

### 风险1：产业链数据不完整
- **应对**：从核心领域开始（AI、云计算、金融），逐步扩展
- **应对**：引入用户贡献机制，允许用户补充产业链数据

### 风险2：验证数据周期长
- **应对**：7天短周期验证 + 30天长周期验证
- **应对**：引入用户即时反馈，缩短学习周期

### 风险3：过度拟合历史数据
- **应对**：定期评估模型泛化能力
- **应对**：引入探索机制，避免只推荐"已验证成功"的股票

---

## 八、下一步行动

**立即执行（本周）：**
1. 创建产业链知识图谱初始数据（AI、云计算、金融、游戏）
2. 搭建SQLite数据库，实现映射规则管理
3. 重构ConceptStockAnalyzer，从硬编码迁移到数据库驱动

**后续优化（下周）：**
1. 实现验证系统，追踪推荐后的股价表现
2. 开发用户反馈接口（投票、评论）
3. 发布v4.0版本，引入置信度标签和验证数据展示
